# to work on

- recover the one missing closure target.
- recover root captures and the `17:51` / `19:65` keys.
- fix replay divergence after the compiled candidate starts.
- scalarize dynamic register frames.
- move the c++ oracle into this repo.
- support versions newer than `v14.7`.

