# Netflix Web Launcher

This local macOS app launches Netflix in a supported browser instead of trying to run the iOS IPA or Windows Store app.

It prefers Chrome app-window mode, then Edge app-window mode, then Safari.
